---
interface Props {
  text: string;
}

const { text } = Astro.props;
---

<span class="rounded-full border border-[var(--line-strong)] bg-[color-mix(in_oklab,var(--accent-soft)_18%,transparent)] px-3 py-1 text-xs font-medium tracking-wide text-[var(--text-primary)] uppercase">
  {text}
</span>
