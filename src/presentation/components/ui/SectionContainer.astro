---
interface Props {
  id: string;
  className?: string;
}

const { id, className = '' } = Astro.props;
---

<section id={id} class={`reveal-section mx-auto max-w-6xl px-4 py-12 sm:px-6 sm:py-14 md:px-10 md:py-20 ${className}`}>
  <slot />
</section>
