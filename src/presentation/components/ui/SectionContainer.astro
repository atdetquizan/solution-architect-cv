---
interface Props {
  id: string;
  className?: string;
}

const { id, className = '' } = Astro.props;
---

<section id={id} class={`reveal-section mx-auto max-w-6xl px-6 py-16 md:px-10 md:py-20 ${className}`}>
  <slot />
</section>
